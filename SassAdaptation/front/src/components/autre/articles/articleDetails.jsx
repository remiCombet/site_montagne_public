import React from 'react'

const ArticleDetails = ({ article, onClose, onUpdate }) => {
    // console.log('Article:', article, onClose, onUpdate);

    return (
        <div>
            
        </div>
    )
};

export default ArticleDetails;
